export interface StudyResearch {intent:string;error?:string;results:{title:string;href?:string|null;date?:string|null;kind?:string;doc_id?:number}[]}
export function ResearchResults({research}:{research?:StudyResearch}){const items=research?.results.filter(r=>r.href&&(/^(https?:\/\/|\/doc\/)/.test(r.href)))??[];if(!items.length)return null;return <details className="my-3 rounded-xl border p-3 text-caption" open><summary className="font-medium">찾은 자료 {items.length}개</summary><ul className="mt-2 space-y-2">{items.slice(0,3).map(r=><li key={r.href}><a className="text-primary hover:underline" href={r.href!} target="_blank" rel="noreferrer">{r.title} ↗</a><span className="block text-muted-foreground">{r.kind==='web'?'웹 원문':'수집 자료'}{r.date?` · ${r.date.slice(0,10)}`:''}</span></li>)}</ul>{items.length>3&&<details className="mt-2"><summary>나머지 {items.length-3}개 보기</summary><ul className="mt-2 space-y-2">{items.slice(3).map(r=><li key={r.href}><a href={r.href!} target="_blank" rel="noreferrer" className="text-primary hover:underline">{r.title} ↗</a></li>)}</ul></details>}</details>}
