fastapi
uvicorn[standard]
python-dotenv
opendartreader==0.2.2  # 0.3.x는 패키지 구조 변경으로 `import OpenDartReader` 불가
pykrx
yfinance          # 주가 수집 (krx_service — pykrx 차단 시 대체 경로)
aiosqlite
pandas
httpx
beautifulsoup4
lxml
feedparser
requests
markitdown
finance-datareader  # 업종(KSIC)·시장 구분 시딩 (scripts/seed_sectors.py)
sqlite-vec          # 벡터 검색 (pipeline/search.py)
fastembed           # 로컬 임베딩 (다국어 MiniLM 384d, API 키 불필요)
# anthropic  # LLM enrich 활성화 시 주석 해제 (ANTHROPIC_API_KEY 확보 후)
